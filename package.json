{
  "name": "user-management-system-backend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "start": "cd node-mysql-boilerplate-api && npm install && npm start"
  }
}
